// MapTiler API key for the basemap tiles (streets + satellite, light + dark).
//
// Get a free key: sign up at https://cloud.maptiler.com/ , then go to
// Account -> API Keys and copy the default key (or create a new one).
// MapTiler's free tier is generous for a project this size and doesn't
// require a credit card. Paste your key below — like the Supabase anon
// key, this is meant to be used client-side.
export const MAPTILER_API_KEY = 'qQg5ilI8ZO5ICPPLFxGg';
